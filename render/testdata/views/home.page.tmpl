<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <h1>Welcome{{if .StringMap.title}} to {{.StringMap.title}}{{end}}</h1>
    <p>This is the home page test template.</p>
    {{if .Data}}
        <p>Data: {{.Data}}</p>
    {{end}}
</body>
</html>